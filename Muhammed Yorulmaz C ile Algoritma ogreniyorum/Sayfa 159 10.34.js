function calculate(a,b,c) {
    if((a*a) == ((b*b) +(c*c))) {
        document.write("bu bir dik üçgendir.")
    } else {
        document.write("dik ucgen degildir.")
    }
}

// ilk değer hipotenüs olandır.

calculate(5,4,3)