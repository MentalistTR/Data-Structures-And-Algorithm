let x = Number(prompt("lütfen 3 haneli bir sayi giriniz."));

if(Math.pow(Number(x[0]),Number(x[0])) && Math.pow(Number(x[1]),Number(x[1])) && Math.pow(Number(x[2]),Number(x[2]))) {
    document.write("bu bir .. sayisidir.")}
    else {
    document.write("bu hicbirseydir.")
    }
